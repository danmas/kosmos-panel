# 🎨 Инструкция по созданию новых тем (скинов) для Kosmos Panel

В Kosmos Panel используется централизованная система темизации на базе CSS Variables. Чтобы добавить новый скин, нужно выполнить два простых шага.

---

## Шаг 1: Определение CSS-переменных

Откройте файл `web/themes.css` и добавьте новый блок селектора с атрибутом `[data-theme="ID_ВАШЕЙ_ТЕМЫ"]`.

### Рекомендуемый шаблон для копирования:

```css
/* Моя новая тема */
[data-theme="my-awesome-skin"] {
    /* Фоновые цвета */
    --bg-main: #0a0a0a;      /* Основной фон страницы */
    --bg-secondary: #141414; /* Вторичный фон (хедеры) */
    --bg-card: #1a1a1a;      /* Фон плиток серверов */
    --bg-panel: #0a0a0a;     /* Фон боковых панелей */
    --bg-header: #111111;    /* Фон шапки */
    --bg-input: #000000;     /* Фон полей ввода */
    --bg-term: #000000;      /* Основной фон терминала */
    --bg-hover: rgba(255, 255, 255, 0.05); /* Эффект при наведении */
    --bg-active: rgba(255, 255, 255, 0.1);  /* Эффект при нажатии */
    --bg-tooltip: #222222;   /* Фон всплывающих подсказок */
    --bg-overlay: rgba(0, 0, 0, 0.8);       /* Фон затемнения модалок */
    --bg-modal: #1a1a1a;     /* Фон модальных окон */

    /* Цвета терминала (критически важно для читаемости) */
    --term-bg: #000000;      /* Фон xterm.js */
    --term-fg: #00ff00;      /* Цвет текста xterm.js */

    /* Границы (Borders) */
    --border-main: #333333;  /* Основные границы */
    --border-light: #444444; /* Светлые/разделительные линии */
    --border-focus: #00ffcc; /* Цвет фокуса/выделения */

    /* Текст */
    --text-main: #ffffff;    /* Основной текст */
    --text-muted: #aaaaaa;   /* Приглушенный текст */
    --text-dim: #666666;     /* Очень бледный текст */
    --text-inverted: #000000; /* Текст на светлом фоне (акценте) */

    /* Акцентные цвета */
    --primary: #00ffcc;      /* Основной брендовый цвет */
    --primary-hover: #00cca3; /* Цвет кнопки при наведении */
    --primary-dim: rgba(0, 255, 204, 0.15); /* Прозрачный фон акцента */

    /* Цвета статусов */
    --success: #22c55e;
    --warning: #f59e0b;
    --error: #ef4444;
    --info: #3b82f6;

    /* Цвета логов (Terminal/Logs) */
    --log-ai: #ffa500;
    --log-stdin: #00ff00;
    --log-stdout: #00ffcc;
    --log-stderr: #ef4444;

    /* Скроллбары и табы */
    --scrollbar-thumb: #444444;
    --tab-active-border: #00ffcc;
}
```

---

## Шаг 2: Регистрация темы в JavaScript

Чтобы тема появилась в выпадающем списке выбора в интерфейсе, её нужно добавить в массив `THEMES` в файле `web/theme-manager.js`.

1. Найдите константу `THEMES`:
```javascript
const THEMES = [
    { id: 'dark', name: '🌙 Dark', icon: '🌙' },
    { id: 'light', name: '☀️ Light', icon: '☀️' },
    { id: 'cyberpunk', name: '🤖 Cyberpunk', icon: '🤖' },
    // Добавьте вашу тему сюда:
    { id: 'my-awesome-skin', name: '✨ Awesome Skin', icon: '✨' }
];
```

*   `id`: Должен СТРОГО совпадать с тем, что вы написали в CSS (`data-theme="..."`).
*   `name`: Название, которое увидит пользователь в меню.
*   `icon`: Иконка/Эмодзи для индикации.

---

## Советы по дизайну

1.  **Контрастность**: Всегда проверяйте, чтобы текст был читаемым на выбранных фонах. Особенно в терминале.
2.  **Свечение (Neon)**: Для достижения эффекта свечения (как в Cyberpunk) используйте яркие цвета для `--primary` и `--border-focus`.
3.  **Темные темы**: Если вы делаете темную тему, старайтесь не использовать чисто черный `#000000` для всего. Лучше использовать глубокие оттенки серого или синего для создания визуальной глубины.
4.  **Синхронизация**: Благодаря встроенному `theme-manager.js`, ваша тема автоматически применится ко всем открытым вкладкам и окнам терминала.

---

## Пример: Тема "Matrix"

**CSS:**
```css
[data-theme="matrix"] {
    --bg-main: #000500;
    --bg-secondary: #001000;
    --bg-card: #001500;
    --bg-header: #001000;
    --bg-term: #000000;
    --term-bg: #000000;
    --term-fg: #00ff41;
    --border-main: #003b00;
    --border-light: #008f11;
    --border-focus: #00ff41;
    --text-main: #00ff41;
    --text-muted: #008f11;
    --text-dim: #003b00;
    --primary: #00ff41;
    --primary-hover: #00cc33;
    --primary-dim: rgba(0, 255, 65, 0.1);
}
```

**JS:**
```javascript
{ id: 'matrix', name: '📟 Matrix', icon: '📟' }
```
